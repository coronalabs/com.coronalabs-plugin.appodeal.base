Sources for the plugin `plugin.appodeal.base`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.base" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
